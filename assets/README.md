Place your profile image here as `profile.jpg`.

- Recommended filename: `profile.jpg`
- Recommended size: square (400×400 or larger) for best quality.
- The page references `assets/profile.jpg` by default. If you prefer a different name or path, update the `src` in `index.html`.

Drop your image into this folder and reload the page.